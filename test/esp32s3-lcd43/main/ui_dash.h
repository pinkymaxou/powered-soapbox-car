// ui_dash.h — Tableau de bord véhicule (maquette LVGL). Valeurs animées en démo
// (pas de lecture de vraies données). Construit sur l'écran actif + horloge analogique.
#pragma once

void ui_dash_build(void);   // à appeler après lvgl_port_add_disp_rgb()
